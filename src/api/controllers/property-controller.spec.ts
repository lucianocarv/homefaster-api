import { vi, describe, it, expect } from 'vitest';

vi.mock('./property-controller.ts', () => {
  return {};
});

describe('property-controller tests', async () => {
  it('', async () => {
    const spy = vi.fn();
  });
});
